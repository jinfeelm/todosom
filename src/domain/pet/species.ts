import type { PetSpecies } from '@/domain/gyeol/types';

export const DEFAULT_SPECIES: PetSpecies[] = [
  {
    id: 'som_mong',
    name: '솜몽',
    assetKey: 'som_mong',
    isPaid: false,
    productId: null,
  },
];

export const DEFAULT_SPECIES_ID = 'som_mong';
